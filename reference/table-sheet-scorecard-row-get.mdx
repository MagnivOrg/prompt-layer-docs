---
title: "Get Scorecard Row"
openapi: "GET /api/public/v2/tables/{table_id}/sheets/{sheet_id}/scorecard/rows/{row_index}"
---

Get the scorecard breakdown for one row.

The response includes `step_results` with per-criterion verdicts and evidence.
