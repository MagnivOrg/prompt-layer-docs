---
title: "Create Table"
openapi: "POST /api/public/v2/tables"
---

Create a new Smart Table. A default sheet with one text column is created automatically.

Smart Tables are versioned, multi-sheet tables that can run LLM, code, and comparison columns to generate or evaluate data at scale.
