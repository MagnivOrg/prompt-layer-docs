---
title: "Configure Scorecard"
openapi: "PATCH /api/public/v2/tables/{table_id}/sheets/{sheet_id}/scorecard"
---

Create or update the active scorecard for a sheet.

`name` and `aggregation` are required. `evaluated_column_ids` and `steps` are optional and default to empty lists.
