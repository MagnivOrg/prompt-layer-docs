---
title: "Migrate from Evaluations and Datasets"
description: "Move legacy Dataset, Evaluation, and Report workflows into Tables."
icon: "arrows-rotate"
---

<Warning>
  Legacy Evaluations, Reports, and Datasets remain available for existing projects. Use Tables for new workflows.
</Warning>

Tables combine the pieces that legacy workflows split across Datasets, Evaluations, and Reports.

## Concept mapping

| Legacy concept | Table concept |
| --- | --- |
| Dataset group | Table |
| Dataset row | Table row |
| Dataset column | Text column |
| Dataset version | Sheet version |
| Evaluation pipeline | Sheet with computed columns |
| Evaluation step | Computed column |
| Report run | Recalculation operation |
| Report output | Computed cell output |
| Report score card | Sheet score configuration |
| Report history | History, versions, and score history |

## Migration checklist

1. Create a Table for the workflow.
2. Import the legacy dataset with CSV upload, or import request history directly.
3. Recreate each evaluation step as a computed column.
4. Map Prompt Template variables to source columns.
5. Add comparison, assertion, extraction, or code columns as needed.
6. Configure the sheet score.
7. Rerun stale or pending cells.
8. Review the score, history, and analytics.
9. Move automation to the Tables API.

## Dataset from CSV

Old flow:

1. Create a Dataset Group.
2. Upload a CSV or JSON file.
3. Save a Dataset Version.
4. Attach the Dataset Version to an Evaluation pipeline.

Table flow:

1. Create a Table.
2. Click **Upload**.
3. Choose **From computer**.
4. Upload the CSV into the sheet.
5. Add computed columns and scoring in the same sheet.

<Frame>
  <img src="/images/tables/polished/02-upload-menu.png" alt="Upload menu for importing a CSV file into a Table" />
</Frame>

## Dataset from request history

Old flow:

1. Filter request logs.
2. Save results into a Dataset Version.
3. Attach the Dataset Version to an Evaluation pipeline.

Table flow:

1. Create a Table.
2. Click **Upload**.
3. Choose **From request history**.
4. Filter and select requests.
5. Click **Add to Sheet**.
6. Add prompt, comparison, assertion, and score columns in the same sheet.

<Frame>
  <img src="/images/tables/polished/03-request-history-import-full.png" alt="Import request history directly into a Table sheet" />
</Frame>

## Evaluation pipeline

Old flow:

1. Define an Evaluation pipeline.
2. Attach a Dataset Version.
3. Add evaluation steps.
4. Run a Report.
5. Review outputs and score cards.

Table flow:

1. Put inputs in text columns.
2. Add a computed column for each step.
3. Configure source mappings for Prompt Template columns.
4. Run the computed columns.
5. Use stale-cell indicators to decide what needs rerunning.
6. Configure the sheet score.

<Frame>
  <img src="/images/tables/polished/07-source-mapping.png" alt="Prompt Template source mappings used to replace legacy evaluation step inputs" />
</Frame>

## Report run

Old flow:

1. Start a Report run.
2. Wait for row outputs.
3. Review failures.
4. Open a score card.

Table flow:

1. Run the Table, a computed column, or selected stale work.
2. Inspect completed, failed, pending, or stale cells in place.
3. Open **Score** for aggregate results.
4. Open **Analytics** to debug request-level behavior.
5. Open **History** to compare versions.

<Frame>
  <img src="/images/tables/polished/16-score-configuration.png" alt="Score configuration replacing a legacy report score card" />
</Frame>

## Automation mapping

| Legacy automation | Tables API |
| --- | --- |
| Create Dataset Group | [Create Table](/reference/tables-create) |
| Upload Dataset file | [Create file import](/reference/table-sheet-imports-file-create) |
| Create Dataset from request logs | [Create request-log import](/reference/table-sheet-imports-request-logs-create) |
| Add Evaluation step | [Create column](/reference/table-sheet-columns-create) |
| Run Report | [Create operation](/reference/table-sheet-operations-create) |
| Poll Report status | [Get operation](/reference/table-sheet-operations-get) |
| Read Report score | [Get sheet score](/reference/table-sheet-score-get) |
| Save checkpoint | [Create sheet version](/reference/table-sheet-versions-create) |

## Recommended order

Migrate one workflow at a time:

1. Start with a workflow that already has stable inputs.
2. Rebuild it as a Table manually.
3. Confirm the outputs and score match the old workflow.
4. Move the automation to Tables APIs.
5. Keep the legacy workflow read-only until the Table is trusted.
